package com.phundal.numverify.buildlogic

/** The single source of truth for the Android API levels every module compiles against. */
internal object AndroidSdk {
    const val COMPILE_SDK = 37
    const val MIN_SDK = 24
    const val TARGET_SDK = 36
}
